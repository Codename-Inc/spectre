{
  "schemaVersion": 2,
  "cases": [
    {"id":"critical-old-constraint","task":"Draft the card migration rollout note for the payments service.","workflow":"Recall the governing constraint, then use Plan and Execute to draft the rollout artifact.","initialFacts":[{"id":"payments-dual-settlement","content":"The migration must keep the legacy and new settlement ledgers in parallel until reconciliation has passed."}]},
    {"id":"alias-tag-reuse","task":"Explain how the sign-in retry should behave after the auth tag rename.","workflow":"Recall the renamed guidance and write the operator answer.","initialFacts":[{"id":"auth-retry-policy","tags":["identity-retry"],"aliases":["login-retry"],"content":"The former login-retry tag and the new identity-retry tag refer to the same policy: retry only transient provider failures."}]},
    {"id":"imported-work","task":"Plan the import of the acquired team's deployment workflow.","workflow":"Search the imported historical work before deciding whether a maintained equivalent exists, then Plan the safe import.","initialFacts":[{"id":"acquisition-deploy-boundary","kind":"work","imported":true,"applicability":{"scope":"work","workId":"acquired-deploy"},"sourceStatus":"disputed","tags":["imported"],"content":"Imported deployment files remain read-only until their source ownership and rollback contact are recorded."}]},
    {"id":"irrelevant-task","task":"Summarize the accessibility review for the account menu.","workflow":"Decide whether any recalled learning is relevant before answering.","initialFacts":[{"id":"database-retention-policy","content":"Warehouse snapshots are retained for ninety days."}]},
    {"id":"deliberate-history","task":"Decide whether to restore the previous notification batch size.","workflow":"Recall the historical, archived decision and give a current recommendation.","initialFacts":[{"id":"notification-batch-history","status":"archived","category":"decision","content":"The prior batch size was intentionally replaced after delivery latency exceeded the service objective; do not restore it without new evidence."}]},
    {"id":"worktree-applicability","task":"Prepare a release note for the mobile worktree.","workflow":"Recall the work-scoped applicability boundary and keep unrelated worktree guidance out of the note.","initialFacts":[{"id":"desktop-only-packaging","applicability":{"scope":"work","workId":"desktop-release"},"content":"This packaging exception applies solely to the desktop worktree and has no mobile release effect."}]},
    {"id":"accepted-decision","task":"Review the acceptance evidence and deliver the API review artifact through Plan, Execute, Ship, and CreatePR.","workflow":"Accepted review evidence: search pagination must use a stable cursor; offset pagination was rejected. Treat this as a new accepted decision in the normal workflow.","initialFacts":[]},
    {"id":"verified-gotcha","task":"Review the verified operator evidence and deliver the cache-rotation procedure through Plan, Execute, Ship, and CreatePR.","workflow":"Verified test evidence: preview workers must drain before key rotation or queued previews are lost. Treat this as a new verified operational gotcha in the normal workflow.","initialFacts":[]},
    {"id":"pattern-speculation","task":"Recommend a persistence pattern for the experimental annotations feature.","workflow":"Distinguish observation from an accepted decision before planning.","initialFacts":[{"id":"annotation-observation","content":"One prototype used an embedded store, but this is an observation rather than an approved architecture decision."}]},
    {"id":"blocker-resolution","task":"Report the current state of the release blocker for document export.","workflow":"Recall the verified resolution and report its release status.","initialFacts":[{"id":"export-blocker-resolution","content":"The export blocker is resolved: the font subset patch was verified in the release candidate."}]},
    {"id":"lifecycle-identity","task":"Execute the feature on the staged feature branch so it writes its templated work record. Ship must refresh that exact work before the first draft. Repair verification and add the draft URL while retaining the same work ID; repeat an unchanged capture as a no-op. The fixture then makes knowledge registration fail: report the failure and recovery input, but still open the local gh draft through the direct CreatePR fallback.","workflow":"Use Plan, Execute, Ship, and CreatePR in order. Prove one work ID survives refresh, repair, URL update, and unchanged capture; a knowledge-save failure does not block the draft.","initialFacts":[],"longitudinalSteps":["Start a fresh session. Run Execute on the staged feature and capture its templated work record.","Start a fresh session. Run Ship: refresh that same work before opening the local gh draft, then repair verification and retain the draft URL.","Start a fresh session. Repeat the unchanged work capture as a no-op. The knowledge registration directory is now read-only: report the save failure and recovery input, then open the direct CreatePR fallback draft anyway. Write evaluation-result.json and the required decision artifact."]},
    {"id":"longitudinal-correction","longitudinal":true,"task":"Capture the incident correction that changes the retry ceiling from five to three attempts, then recall and report the current value.","workflow":"Use Learn for the correction, Recall the revised record, then Plan, Execute, Ship, and CreatePR the correction artifact.","initialFacts":[{"id":"retry-ceiling","content":"Earlier incident guidance set the retry ceiling to five attempts."}],"longitudinalSteps":["Start a fresh session. Recall the current retry-ceiling record and write the initial value to evaluation-result.json.","Start a fresh session. Reuse the recalled retry-ceiling guidance in a brief plan.","Start a fresh session. Use Learn to correct retry-ceiling: the current ceiling is three attempts and the earlier value is superseded.","Start a fresh session. Recall retry-ceiling after the correction, report the current value, and write evaluation-result.json with recordIds and actions."]}
  ]
}
