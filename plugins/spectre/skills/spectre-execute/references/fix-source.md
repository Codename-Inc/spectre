# Fix-source delta

Load after a bug-report path/root or content identifying `Bug`, `Bug Root`, root cause, repair boundary, and verification wins source resolution. Execute owns every other step.

- The report is authority; its parent is `BUG_ROOT`; hash-bind derivatives. In shared references, read `PLAN_SOURCE` as `FIX_SOURCE`, `FEATURE_ROOT` as `BUG_ROOT`, and Feature labels as Bug labels.
- Never invoke `spectre-plan_review`. Reuse routing or classify once; do not reopen diagnosis.
- ATOMIC/DIRECT rejoins the existing coarse-map path. STRUCTURED rejoins the existing task-generation path without Plan Review evidence or Task Review.
- Skip the Plan telemetry join and `plan.execution_outcome`.
- Return `PREPARATION_READY`.
