#!/bin/sh
# Thin wrapper so skills can call `caspar-register` from Bash.
# Delegates to the hook registration runtime.
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
exec node "$SCRIPT_DIR/../hooks/scripts/register_learning.mjs" "$@"
